// import { PayloadAction, createSlice } from "@reduxjs/toolkit";
// import { UserModel } from "../../Models/UserModel";
// import { acceptFriendRequest, declineFriendRequest, sendFriendRequest } from "./FriendsRequestsSlice";

// function initUsers(state: Map<string, UserModel>, action: PayloadAction<Map<string, UserModel>>): Map<string, UserModel> {
//     return action.payload;
// }

// function updateUser(state: Map<string, UserModel>, action: PayloadAction<UserModel>): Map<string, UserModel> {
//     const newState = new Map(state);
//     if (newState.has(action.payload._id))
//         newState.set(action.payload._id, action.payload)
//     return newState;
// }

// function deleteUser(state: Map<string, UserModel>, action: PayloadAction<string>): Map<string, UserModel> {
//     const newState = new Map(state);
//     if (newState.has(action.payload))
//         newState.delete(action.payload)
//     return newState;
// }

// const userSlice = createSlice({
//     name: "users",
//     initialState: new Map(),
//     reducers: { initUsers, updateUser, deleteUser, acceptFriendRequest, sendFriendRequest, declineFriendRequest },
// });

// export const userActionCreators = userSlice.actions;

// export const userReducersContainer = userSlice.reducer;
export class ffff{
    
}